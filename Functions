int max_of_four(int a , int b , int c, int d)
{
    int temp{};
    int arr[]{a,b,c,d};
    for(int i=0;i<4;i++)
    {
        if(temp<arr[i])
        {
            temp=arr[i];
        }
    }
    return temp;
}
