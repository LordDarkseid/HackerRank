#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector <int> vec;
    int n,a;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>a;
        vec.push_back(a);
    }
    for(int i=n-1;i>=0;i--)
    {
        cout<<vec[i]<<" ";
    }
    return 0;
}
